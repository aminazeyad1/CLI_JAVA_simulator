public class Parser {
    String commandName;
    String[] args;

    //This method will divide the input into commandName and args, where "input" is the string command entered by the user
    //It also checks validation of the input/number of args if valid --> return true, if not --> prints error message then return false
    public boolean parse(String input){
        
    }
    public String getCommandName(){
        return commandName;
    }
    public String[] getArgs(){
        return args;
    }
}
